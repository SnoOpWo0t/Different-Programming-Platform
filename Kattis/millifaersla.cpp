#include<bits/stdc++.h>
using namespace std ;
int main ()
{
    int a,b,c;
    cin >> a >> b >> c;
  //  int max = max(max(a,b),c);
    int minv = min(min(a,b),c);
   if(minv ==a)
   cout<<"Monnei"<<endl;
   else if(minv ==b)
   cout<<"Fjee"<<endl;
    else
    cout<<"Dolladollabilljoll"<<endl;

}